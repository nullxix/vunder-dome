import React from 'react'
import styled from 'styled-components'

const Vs = styled.div`
    color: black;
`

export default props => {
    return (
        <Vs>
            ~VS~
        </Vs>
    )
}